export function CData({ H, S, V }) {
    return (
        <div className="font-semibold text-4xl">
            <h1>HUE: {H}</h1>
            <h1>SATURATION: {S}</h1>
            <h1>VALUE: {V}</h1>
            -----------------------------
        </div>
    );
}
